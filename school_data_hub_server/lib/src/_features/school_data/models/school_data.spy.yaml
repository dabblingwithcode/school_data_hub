class: SchoolData
table: school_data
fields:
  name: String
  officialName: String
  extraName: String?
  address: String
  ## TODO: zipCode is optional to avoid a forced migration
  ## This should be fixed in a future migration
  zipCode: String?
  ## TODO: city is optional to avoid a forced migration
  ## This should be fixed in a future migration
  city: String?
  schoolNumber: String
  telephoneNumber: String
  email: String
  website: String
  principalName: String?
  logo: HubDocument?, relation(optional)
  officialSeal: HubDocument?, relation(optional)
  
